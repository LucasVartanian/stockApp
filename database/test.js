const { connect } = require("../src/routes/user")

ventas = [{
    fecha, producto, cant, monto
},{},{},{}
]

{fecha, producto, cant, monto}
{nro, saleCode, }

ventas.map((prod) => {
    conn.query(INSERT INTO sales ?, [prod])
})